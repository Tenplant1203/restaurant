## Context

This project uses Django, uv, GitHub, OpenSpec, and Codex.

## Goals / Non-Goals

**Goals:** Use simple project rules, OpenSpec plans, skills, tests, and PRs.

**Non-Goals:** Do not change application features or the database.

## Decisions

Keep workflow files in the repository. Use `uv run` for project commands.

## Risks / Trade-offs

Keep the workflow small to avoid unnecessary work.
