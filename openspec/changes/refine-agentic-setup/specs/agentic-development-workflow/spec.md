## ADDED Requirements

### Requirement: Agent workflow

The project SHALL provide a simple workflow for AI-assisted development.

#### Scenario: Start a task

- **WHEN** a new task starts
- **THEN** the agent reads `AGENTS.md` and the OpenSpec plan
